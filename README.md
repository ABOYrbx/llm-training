# LLM Training from Scratch

Train a GPT-2 style language model from scratch using HuggingFace.

## Installation
```bash
pip install -r requirements.txt
```

## Usage
```bash
python train.py
```

## Docker
```bash
docker build -t llm-training .
docker run llm-training
```
